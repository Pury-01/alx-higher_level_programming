#!/usr/bin/node

// replaces value 12 with 89

const myObject = {
  type: 'object',
  vaue: 12
};

console.log(myObject);

// modify the value of myObject
myObject.value = 89;

console.log(myObject);
